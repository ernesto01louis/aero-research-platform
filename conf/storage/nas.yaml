# Stage 09 (ADR-011) — on-prem NAS DVC remote profile.
#
# TrueNAS-SCALE MinIO/S3 app on the dedicated hardware NAS at 192.168.2.60. NOTE:
# the NAS got its OWN identity — it did NOT re-IP to the old TrueNAS VM's .100 (the
# parallel-cutover runbook's re-IP assumption was superseded). The aero data was
# migrated VM104:f3/aero -> NAS:tank/aero on 2026-06-30; the MinIO app backs this
# remote. Running MinIO/S3 natively on ZFS sidesteps the NFS-atomicity/root-squash
# reason ADR-004 kept MinIO off NFS. Switch the default to this (conf/config.yaml
# `- storage: nas`) once ready; flipping cloud->NAS is config + creds only, zero code change.

dvc_remote: aero-nas
s3_endpoint: http://192.168.2.60:9000
bucket: aero-dvc
