"""Outer-loop hypothesis prompt templates (stub).

Outer-loop ideation in the three-loop architecture (see VISION.md).
Stage 4+ lands ``hypothesis_prompts.py`` — Jinja2 templates that
the planner agent reads when proposing the NEXT campaign given the
evidence bundles from the prior round.
"""
from __future__ import annotations

__all__: list[str] = []
