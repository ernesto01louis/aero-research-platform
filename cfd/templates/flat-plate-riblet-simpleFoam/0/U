FoamFile
{
    version     2.0;
    format      ascii;
    class       volVectorField;
    object      U;
}

dimensions      [0 1 -1 0 0 0 0];

internalField   uniform (1 0 0);

boundaryField
{
    inlet
    {
        type            freestreamVelocity;
        freestreamValue uniform (1 0 0);
    }
    outlet
    {
        type            inletOutlet;
        inletValue      uniform (1 0 0);
        value           uniform (1 0 0);
    }
    top
    {
        type            slip;
    }
    // Riblet case: snappyHexMesh adds the `riblets` patch from the STL
    // solid name. Smooth case: only `bottomWall` exists. Regex matches both.
    "(bottomWall|riblets)"
    {
        type            noSlip;
    }
    frontPeriodic
    {
        type            cyclic;
    }
    backPeriodic
    {
        type            cyclic;
    }
}
