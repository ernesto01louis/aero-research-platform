---
# Ansible Galaxy collections for the Stage 02 playbooks.
# Install:  ansible-galaxy collection install -r requirements.yml
# Pins are recorded in docs/adrs/ADR-002-proxmox-topology.md.
collections:
  - name: community.general
    version: "12.6.1"
  - name: ansible.posix
    version: "2.1.0"
